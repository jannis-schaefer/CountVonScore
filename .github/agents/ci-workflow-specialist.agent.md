---
description: "TEMPLATE (Phase 2): Own CI workflow design, required/optional gates, and release pipeline quality policy."
name: "CIWorkflowSpecialist"
tools: [read, search, edit]
user-invocable: true
---

# CI Workflow Specialist Agent (Template)

## Runtime Tuning

- Model: user-selectable per run (`model`). Default: `GPT-5.3-Codex (copilot)`.
- Reasoning depth: user-selectable per run (`reasoningDepth`: `low | medium | high`). Default: `medium`.

## Status

Template only. Implement in phase 2.
